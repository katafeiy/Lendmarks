import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Привет, Константин!")
                .font(.title)
                .foregroundColor(.green)
        }
    }
}

#Preview {
    ContentView()
}
